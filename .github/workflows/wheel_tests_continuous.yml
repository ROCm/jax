# CI - Wheel Tests (Continuous) - ROCm only
#
# Stripped-down version of the continuous workflow for testing ROCm clone_main_xla plumbing.
# Only jobs that run on AMD runners are kept. Build jobs on non-AMD runners
# (build-jax-artifact, build-jaxlib-artifact) are removed. run-pytest-rocm uses
# PyPI jaxlib instead of GCS artifacts. run-bazel-test-rocm builds from source
# so it is fully self-contained.

name: CI - Wheel Tests (Continuous)
permissions:
  id-token: write
  contents: read
  actions: read

on:
  workflow_dispatch: # allows triggering the workflow run manually

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: ${{ !contains(github.ref, 'release/') && github.ref != 'main' }}

jobs:
  build-rocm-artifacts:
    uses: ./.github/workflows/build_rocm_artifacts.yml
    permissions:
      id-token: write
      contents: read
      actions: read
    strategy:
        fail-fast: false
        matrix:
          runner: ["linux-x86-64-1gpu-amd"]
          artifact: ["jax-rocm-plugin", "jax-rocm-pjrt"]
          python: ["3.11", "3.12", "3.13", "3.14"]
          rocm-version: ["7"]
          exclude:
            - artifact: "jax-rocm-pjrt"
              python: "3.12"
            - artifact: "jax-rocm-pjrt"
              python: "3.13"
            - artifact: "jax-rocm-pjrt"
              python: "3.14"
    name: "Build ${{ format('{0}', 'ROCm') }} artifacts"
    with:
      runner: ${{ matrix.runner }}
      artifact: ${{ matrix.artifact }}
      python: ${{ matrix.python }}
      rocm-version: ${{ matrix.rocm-version }}
      clone_main_xla: 1
      upload_artifacts_to_s3: true
      s3_upload_uri: 's3://jax-ci-amd/rocm-wheels/wheel-tests-continuous/${{ github.run_number }}/${{ github.run_attempt }}'

  run-pytest-rocm:
    if: ${{ !cancelled() }}
    needs: [build-rocm-artifacts]
    uses: ./.github/workflows/pytest_rocm.yml
    strategy:
        fail-fast: false
        matrix:
          runner: ["linux-x86-64-1gpu-amd", "linux-x86-64-4gpu-amd", "linux-x86-64-8gpu-amd"]
          python: ["3.11"]
          rocm: [
            {version: "7.2.0", tag: "rocm720"},
          ]
    name: "Pytest ROCm (JAX artifacts version = ${{ format('{0}', 'head') }})"
    with:
      runner: ${{ matrix.runner }}
      python: ${{ matrix.python }}
      rocm-version: ${{ matrix.rocm.version }}
      rocm-tag: ${{ matrix.rocm.tag }}
      jaxlib-version: "head"
      gcs_download_uri: 'gs://jax-nightly-artifacts/latest'
      s3_download_uri: 's3://jax-ci-amd/rocm-wheels/wheel-tests-continuous/${{ github.run_number }}/${{ github.run_attempt }}'

  run-bazel-test-rocm:
    if: ${{ !cancelled() }}
    uses: ./.github/workflows/bazel_rocm.yml
    strategy:
        fail-fast: false
        matrix:
          runner: ["linux-x86-64-4gpu-amd", "linux-x86-64-8gpu-amd"]
          python: ["3.11"]
          rocm-version: ["7"]
          enable-x64: [0]
    name: "Bazel ROCm tests (JAX artifacts version = ${{ format('{0}', 'head') }})"
    with:
      runner: ${{ matrix.runner }}
      python: ${{ matrix.python }}
      rocm-version: ${{ matrix.rocm-version }}
      enable-x64: ${{ matrix.enable-x64 }}
      build_jaxlib: "wheel"
      build_jax: "true"
      jaxlib-version: "head"
      run_multiaccelerator_tests: "false"
      clone_main_xla: 1
